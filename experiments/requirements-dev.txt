# Validation-only dependencies. These are NOT needed to reproduce any number,
# figure, or table in the paper -- the shipped pipeline (common/stats.py) is
# pure-Python/numpy on purpose so a fresh clone stays lightweight. These
# packages are used only by tests/test_stats_vs_scipy.py, which cross-checks
# the self-implemented Mann-Whitney U, its tie-corrected normal approximation,
# the rank-biserial effect size, and the Holm-Bonferroni correction against
# reference implementations (three-referees review fix #5). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
scipy>=1.11
statsmodels>=0.14
